# 📦 Windows 网络配置工具打包说明

本文档提供了使用命令行直接打包不同版本的方法，涵盖绿色免安装便携版与 MSI 安装包。

---

## 🚀 方法1：使用 Cargo 直接构建（推荐便携版）

构建前端后直接由 Cargo 编译生成独立的可执行文件（免安装便携版）。

### 🛠️ 1. 构建 64 位便携版

```bash
# 1. 构建前端
npm run build
# 或使用 yarn: yarn build

# 2. 构建后端 Release
cd src-tauri
cargo build --release
cd ..
```

构建完成后，可执行文件位于：
`src-tauri/target/release/tauri-vue-20250419.exe`

也可以直接双击运行根目录下的 `build-app.bat` 脚本一键完成构建。

### 🛠️ 2. 构建 32 位便携版

```bash
# 1. 安装 32 位 Rust 编译目标 (只需安装一次)
rustup target add i686-pc-windows-msvc

# 2. 构建前端
npm run build

# 3. 构建 32 位后端 Release
cd src-tauri
cargo build --release --target i686-pc-windows-msvc
cd ..
```

构建完成后，可执行文件位于：
`src-tauri/target/i686-pc-windows-msvc/release/tauri-vue-20250419.exe`

---

## 🚀 方法2：使用 Tauri CLI 打包（安装包或指定包格式）

> **说明**：Tauri CLI 中 `--bundles` 指定打包格式（如 `msi`、`none`），`--target` 指定底层目标架构（如 `x86_64-pc-windows-msvc` 或 `i686-pc-windows-msvc`）。

### 1. 打包 64 位绿色便携版

```bash
npm run tauri -- build --bundles none
# 或: yarn tauri build --bundles none
```

生成的单文件位于：`src-tauri/target/release/tauri-vue-20250419.exe`

### 2. 打包 64 位安装包（MSI）

```bash
npm run tauri -- build --bundles msi
# 或: yarn tauri build --bundles msi
```

打包完成后，MSI 安装包位于：`src-tauri/target/release/bundle/msi/`

### 3. 打包 32 位安装包（MSI）

```bash
npm run tauri -- build --target i686-pc-windows-msvc --bundles msi
# 或: yarn tauri build --target i686-pc-windows-msvc --bundles msi
```

打包完成后，MSI 安装包位于：`src-tauri/target/i686-pc-windows-msvc/release/bundle/msi/`

---

## ⚠️ 注意事项与环境要求

1. **打包 MSI 安装包需要 WiX Toolset**：
   - 如果未安装 WiX，请从 [WiX Toolset 官网](https://wixtoolset.org/releases/) 下载安装，或运行：
     `choco install wixtoolset -y`
   - 若系统未安装 WiX，可优先使用方法 1 或 `--bundles none` 构建便携版。

2. **32 位目标支持**：
   - 需确保安装了 Visual Studio 的 x86/x64 C++ 构建工具以及 Rust `i686-pc-windows-msvc` 目标。

3. **管理员权限执行说明**：
   - Windows 修改网络适配器 IPv4、网关、DNS、DoH 与 IPv6 绑定属于系统特权操作，运行生成的应用程序时需以管理员权限启动。

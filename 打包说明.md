# Windows 网络配置工具打包说明

更新日期：2026-09-11。当前版本 0.1.0，本轮生成 Windows x64 便携程序、MSI 和 NSIS。32 位、ARM64 构建没有经过本轮验证。

## 1. 环境准备

- Windows x64、PowerShell 7（pwsh）。
- Node.js、Rust MSVC 工具链、Visual Studio C++ 构建工具、Windows SDK。本轮 Node.js 24.16.0、Rust/Cargo 1.91.1 通过构建。
- 项目依赖和锁文件。Tauri CLI 首次生成安装包可能下载 WiX/NSIS，缓存目录必须可写。
- WebView2 Runtime 用于运行程序；它不是已经嵌入便携 EXE 的独立运行时。

```powershell
npx --yes yarn@1.22.22 install --frozen-lockfile
cargo fetch --manifest-path src-tauri/Cargo.toml --locked
```

不要运行 npm install 引入第二份锁文件。npm run 只执行脚本，不改变 Yarn 依赖管理方式。

## 2. 标准打包

在仓库根目录执行：

```powershell
npm run release
```

也可双击或执行 build-app.bat。批处理入口会切换到脚本定位的项目目录，失败返回非零退出码，不依赖调用者的当前目录。

流程由 scripts/build-release.ps1 和 scripts/export-release.ps1 完成：

1. 调用项目本地 Tauri CLI：`build --ci --bundles msi,nsis -- --locked --offline`。
2. Tauri 的 beforeBuildCommand 执行 npm run build，先 vue-tsc 再 Vite。
3. 编译 release 原生程序，再用 WiX/NSIS 生成安装包。
4. 检查本轮产物存在、非空且时间有效；全部检查通过后复制到 releases。
5. 生成 SHA256SUMS.txt 和 release-manifest.json，后者包含产物大小、哈希及构建输入源码指纹。

离线参数只限制 Cargo，不保证首次 WiX/NSIS 下载或安装时 WebView2 下载无需联网。脚本不自动运行完整测试；打包前请执行开发文档中的测试命令。

仅需要便携程序时：

```powershell
pwsh -NoProfile -File scripts/build-release.ps1 -PortableOnly
```

该选项仅导出便携程序，校验清单也仅覆盖此次导出的文件；releases 中此前存在的安装包不会被更新。发布完整版本请使用默认模式。

## 3. 产物位置

| 路径 | 内容 |
|---|---|
| src-tauri/target/release/Windows网络配置工具.exe | Tauri CLI 构建后的原始便携程序 |
| src-tauri/target/release/bundle/msi/Windows网络配置工具_0.1.0_x64_zh-CN.msi | 中文 MSI |
| src-tauri/target/release/bundle/nsis/Windows网络配置工具_0.1.0_x64-setup.exe | NSIS |
| releases/Windows网络配置工具.exe | 便携程序发布副本 |
| releases/Windows_Network_Config_Tool_v0.1.0.exe | 内容相同的英文文件名副本 |
| releases/Windows网络配置工具_0.1.0_x64_zh-CN.msi | MSI 发布副本 |
| releases/Windows网络配置工具_0.1.0_x64-setup.exe | NSIS 发布副本 |
| releases/SHA256SUMS.txt | SHA-256 清单 |
| releases/release-manifest.json | 产物与源码指纹 |

不建议只运行 cargo build 后手工猜测产品名或复用旧安装包；使用统一脚本生成同一轮产物。首次构建后的 target 目录不提交 Git。

## 4. 产物校验

```powershell
Get-FileHash -Algorithm SHA256 -LiteralPath 'releases/Windows网络配置工具.exe'
Get-FileHash -Algorithm SHA256 -LiteralPath 'releases/Windows_Network_Config_Tool_v0.1.0.exe'
Get-AuthenticodeSignature -LiteralPath 'releases/Windows网络配置工具.exe'
```

对比 SHA256SUMS.txt；两份便携程序哈希应一致。当前没有配置代码签名证书，签名状态为未签名，不应把 SHA-256 校验当作发布者身份认证。

同版本重建的文件内容可能变化，本轮继续沿用 0.1.0，用 SHA-256 和源码指纹区分构建。后续正式升级需同步 package.json、Cargo.toml、Cargo.lock 本项目条目和 tauri.conf.json 的版本号，再全量打包。

## 5. 运行与安装验收

主程序没有 requireAdministrator 清单或按需提权 helper。写入网络配置时应右键程序选择“以管理员身份运行”。安装包的权限提示与应用自身权限是两件事。

安装包的 webviewInstallMode 为 downloadBootstrapper。目标机缺少 WebView2 时需要网络下载；企业离线部署需要另行设计运行时分发方案。当前没有固定版运行时随包交付。

构建成功只证明产物生成。以下验收应在可恢复的 Windows 测试环境中进行：

- 普通/管理员启动，原生 WebView 页面加载及错误提示。
- MSI/NSIS 安装、启动、卸载和同版本/升级行为。
- DHCP、静态 IPv4/IPv6、DNS、DoH 应用与读回。
- 可恢复故障注入下的补偿、断网后人工恢复。
- WebView2 已安装与未安装、联网与离线环境。

本轮未执行安装器或真实网络写入；具体已验证内容见 [验证记录](./docs/verification.md)。

## 6. 故障处理

| 现象 | 处理 |
|---|---|
| 缺少前端模块或本地 CLI | 用 Yarn 1.22.22 和 --frozen-lockfile 安装 |
| Cargo offline 找不到依赖 | 联网执行 cargo fetch --locked 后重试 |
| 无法找到 link.exe 或 Windows SDK | 检查 MSVC C++ 构建组件和 SDK |
| WiX/NSIS 下载、解压或缓存失败 | 检查联网、缓存写权限和安全软件；不要在构建失败后复制旧安装包 |
| 产物导出报 stale | 确认当前构建实际生成了对应产物，重新完整构建；不要跳过检查发布旧文件 |
| 开发端口被占用 | 释放 3000 端口后运行 Tauri dev |
| 程序打开但写网络失败 | 检查管理员权限、系统能力及恢复诊断 |

代码推送使用现有 origin 远端；本轮通过仓库 releases 目录分发文件，不自动创建 GitHub Release 或版本标签。

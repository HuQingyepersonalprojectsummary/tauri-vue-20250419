# 📦 Windows 网络配置工具打包说明

本文档详细介绍了使用命令行与构建脚本打包本软件的完整方法，涵盖绿色免安装便携版（`.exe`）与 Windows Installer 安装包（`.msi`），并支持 64 位与 32 位不同系统架构。

---

## 📂 现成发行包（无需手动编译）

如果您不需要修改代码，项目根目录的 `releases/` 文件夹中已为您提供打包好的现成发行文件：

| 文件名称 | 类型 | 说明 |
| :--- | :--- | :--- |
| `releases/Windows网络配置工具.exe` | 绿色便携版 | **首选推荐**，免安装单文件，双击直接运行 |
| `releases/Windows_Network_Config_Tool_v0.1.0.exe` | 英文命名便携版 | 与中文便携版内容一致，适用于非中文环境系统 |
| `releases/Windows网络配置工具_0.1.0_x64_zh-CN.msi` | MSI 安装包 | 标准 Windows 安装向导，支持快捷方式与集中部署 |

---

## 🚀 方法一：使用 Cargo 快速编译（推荐便携版）

先编译前端资源，然后直接通过 Cargo 编译生成单文件免安装绿色便携版。

### 🛠️ 1. 构建 64 位便携版 (x64)

```bash
# 1. 构建前端静态资源
npm run build
# 或使用 yarn: yarn build

# 2. 编译后端 Release
cd src-tauri
cargo build --release
cd ..
```

编译完成后，生成的可执行文件位于：
`src-tauri/target/release/Windows网络配置工具.exe`

> 💡 **小提示**：您也可以直接双击项目根目录下的 **`build-app.bat`** 脚本，一键全自动完成上述构建！

### 🛠️ 2. 构建 32 位兼容便携版 (x86)

```bash
# 1. 安装 32 位 Rust 编译目标 (初次执行需安装)
rustup target add i686-pc-windows-msvc

# 2. 构建前端
npm run build

# 3. 编译 32 位后端 Release
cd src-tauri
cargo build --release --target i686-pc-windows-msvc
cd ..
```

编译完成后，可执行文件位于：
`src-tauri/target/i686-pc-windows-msvc/release/Windows网络配置工具.exe`

---

## 🚀 方法二：使用 Tauri CLI 打包（安装包与多格式构建）

> **参数说明**：
> - `--bundles none`：只生成 `.exe` 便携版单文件。
> - `--bundles msi`：生成 `.msi` 标准安装包（需安装 WiX Toolset）。
> - `--target`：指定底层架构（如 `x86_64-pc-windows-msvc` 或 `i686-pc-windows-msvc`）。

### 1. 打包 64 位便携版
```bash
npx tauri build --bundles none
```
生成单文件位于：`src-tauri/target/release/Windows网络配置工具.exe`

### 2. 打包 64 位标准安装包 (MSI)
```bash
npx tauri build --bundles msi
```
生成的 MSI 安装包位于：`src-tauri/target/release/bundle/msi/Windows网络配置工具_0.1.0_x64_zh-CN.msi`

### 3. 打包 32 位安装包 (MSI)
```bash
npx tauri build --target i686-pc-windows-msvc --bundles msi
```
生成的 MSI 安装包位于：`src-tauri/target/i686-pc-windows-msvc/release/bundle/msi/`

---

## ⚠️ 打包与运行注意事项

1. **MSI 安装包构建依赖 WiX Toolset**：
   - 如果打包 MSI 失败，请检查是否安装了 WiX Toolset。可从 [WiX Toolset 官方站点](https://wixtoolset.org/) 下载安装，或使用包管理器一键安装：
     ```cmd
     choco install wixtoolset -y
     ```
   - 若本地开发机未安装 WiX，可直接使用方法一或 `--bundles none` 快速生成便携版 `.exe`。

2. **静默无窗口运行特性 (CREATE_NO_WINDOW)**：
   - 程序内部所有对 `netsh.exe` 与 `powershell.exe` 的系统调用均已配置 Windows 原生标志位 `0x08000000 (CREATE_NO_WINDOW)`；
   - 运行过程中**绝不会弹出任何黑框或终端窗口闪烁**，保证极佳的用户体验。

3. **管理员运行权限说明**：
   - 修改 Windows 网络适配器的 IP 地址、子网掩码、网关、DNS、DoH 模板及启用/禁用 IPv6 属于系统特权操作；
   - 便携版或安装程序运行时，系统会自动弹出 UAC 提权请求，请确认并以**管理员权限**运行。
